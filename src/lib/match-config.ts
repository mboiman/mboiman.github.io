/**
 * Where the live side of the project match runs (workers/jev-match). Empty
 * until the worker is deployed: the page then offers the examples only and
 * says that the live match is not reachable. A local run can point elsewhere
 * with PUBLIC_MATCH_ENDPOINT=http://localhost:8799 npm run dev.
 */
export const MATCH_ENDPOINT = '';
